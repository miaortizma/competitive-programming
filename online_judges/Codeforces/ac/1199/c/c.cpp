#include<bits/stdc++.h>
using namespace std;

const int N = 4 * 1e5 + 10;

int arr[N];

int main() {
  int n, I;
  cin >> n >> I;
  for (int i = 0; i < n; ++i)
    cin >> arr[i];
  
  sort(arr, arr + n);



  return 0;
}
